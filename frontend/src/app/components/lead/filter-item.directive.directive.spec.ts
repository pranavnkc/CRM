import { FilterItem.DirectiveDirective } from './filter-item.directive.directive';

describe('FilterItem.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterItem.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
